-- ============================================================
-- BUZZ Pro — تاريخ بدء عمل الموظف (أساس لحسابات رصيد أدق مستقبلًا)
-- آمن تمامًا للتشغيل على قاعدة بياناتك الحالية — لن يمسح أو يغيّر أي بيانات موجودة.
-- كل الموظفين الحاليين يحصلون على القيمة NULL افتراضيًا (لا تاريخ محدَّد) — لا كسر لأي حساب قائم.
-- انسخ هذا الملف كاملًا والصقه في SQL Editor في Supabase واضغط Run
-- ============================================================

alter table staff add column if not exists hire_date date;
